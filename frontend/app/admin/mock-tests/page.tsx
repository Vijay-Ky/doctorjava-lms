'use client';
import Link from 'next/link';import { useEffect,useState } from 'react';import { Plus, BookOpenCheck } from 'lucide-react';import { apiFetch } from '@/lib/api/client';import type { TestCard } from '@/types/practice';
export default function MockTestsAdmin(){const [tests,setTests]=useState<TestCard[]>([]);useEffect(()=>{apiFetch<TestCard[]>('/admin/mock-tests').then(setTests).catch(()=>setTests([]))},[]);return <div className="min-h-screen bg-surface px-5 py-8 md:px-8"><div className="mx-auto max-w-7xl"><div className="flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><Link href="/admin" className="text-xs font-semibold text-muted hover:text-purple">← Admin</Link><h1 className="font-display mt-2 text-3xl font-bold text-ink">Mock Tests</h1><p className="mt-1 text-sm text-muted">Create a test and attach questions from the Question Bank.</p></div><Link href="/admin/mock-tests/new" className="inline-flex items-center justify-center gap-2 rounded-full bg-ink px-5 py-3 text-xs font-bold text-white hover:bg-purple"><Plus size={15}/> Create Mock Test</Link></div><div className="mt-7 grid gap-4 md:grid-cols-2">{tests.map(t=><div key={t.id} className="rounded-3xl border border-ink/5 bg-white p-6 shadow-soft"><div className="flex items-start justify-between gap-3"><div><div className="text-[11px] font-bold uppercase tracking-[.14em] text-purple">{t.subject}</div><h2 className="font-display mt-2 text-lg font-bold text-ink">{t.title}</h2><p className="mt-2 text-sm leading-6 text-muted">{t.description}</p></div><BookOpenCheck size={20} className="text-purple"/></div><div className="mt-5 flex flex-wrap gap-2 text-xs text-muted"><span className="rounded-full bg-surface px-3 py-1.5">{t.totalQuestions} questions</span><span className="rounded-full bg-surface px-3 py-1.5">{t.durationMinutes} min</span><span className="rounded-full bg-purple/5 px-3 py-1.5 text-purple">+{t.marksPerQuestion} / -{t.negativeMarks}</span></div></div>)}</div>{tests.length===0&&<div className="mt-5 rounded-3xl border border-dashed border-ink/10 bg-white p-12 text-center text-sm text-muted">No mock tests yet.</div>}</div></div>}
